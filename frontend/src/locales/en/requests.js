import { requests } from '../constanst';

const translation = {
  [requests.pageHead]: 'Requests',
  [requests.username]: 'User',
  [requests.content]: 'Request',
  [requests.type]: 'Type',
  [requests.date]: 'Date',
  [requests.isCompleted]: 'Completed',
};

export default translation;
