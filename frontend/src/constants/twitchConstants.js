import { common } from 'locales/index';

export const getTwitchRoles = (t) => [
  t(common.everyone),
  t(common.moderator),
  t(common.subscriber),
  t(common.vip),
  t(common.broadcaster),
];

export const test = 'test';
